---
import Item from '~/components/realisations/ListItem.astro';
import type { Realisation } from '~/types';

export interface Props {
  posts: Array<Realisation>;
}

const { posts } = Astro.props;
---

<ul>
  {
    posts.map((post) => (
      <li class="mb-12 md:mb-20">
        <Item post={post} />
      </li>
    ))
  }
</ul>
